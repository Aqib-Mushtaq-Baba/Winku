@model IEnumerable<FreindRequestsVM>

@{
    ViewBag.Title = "Notifications";
}

<h1>Notifications</h1>

@* @if (Model.Any())
{
    <div class="test">    
        @foreach (var item in Model)
        {
            <h3>@item.UserName</h3>

            @if (item.ProfileImage != null)
            {
                <img src="~/UserProfilePictures/@item.ProfileImage" width="100" height="100"/>
            }
            else
            {
                <img src="~/UserProfilePictures/DummyImage.png" width="100" height="100" />
            }

            <a onclick="accept(this);" user-id="@item.Id" class="btn btn-success Acceptbtn">Accept</a>
            <a onclick="decline(this);" user-id="@item.Id" class="btn btn-danger Declinebtn">Decline</a>
            <hr />
        }
    </div>
}
else
{
    <h1>You don't have any Notifications</h1>
} *@

@* @if (Model.Any())
{ *@
    <div class="test">
        @foreach (var item in Model)
        {
            <div>
                <h3>@item.UserName</h3>

                @if (item.ProfileImage != null)
                {
                    <img src="~/UserProfilePictures/@item.ProfileImage" width="100" height="100" />
                }
                else
                {
                    <img src="~/UserProfilePictures/DummyImage.png" width="100" height="100" />
                }

                <a onclick="accept(this);" user-id="@item.Id" class="btn btn-success Acceptbtn">Accept</a>
                <a onclick="decline(this);" user-id="@item.Id" class="btn btn-danger Declinebtn">Decline</a>
                <hr />
            </div>
        }
    </div>
@* }
else
{ *@
@if (!Model.Any())
{
    <div class="mcq">
                <h1>You don't have any Notifications</h1>
        </div>    
}
    
@* } *@

<script src="~/js/freind.js"></script>
